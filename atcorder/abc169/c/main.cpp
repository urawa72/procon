#include <bits/stdc++.h>
using namespace std;

#define ALL(v) v.begin(), v.end()
#define V vector
#define P pair
typedef long long ll;

int main() {
    long double a; cin >> a;
    long double b; cin >> b;
    ll ans = floor(a * b);

    cout << ans << endl;
}
