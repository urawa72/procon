#include <bits/stdc++.h>
#include <iomanip>
using namespace std;

#define all(v) v.begin(), v.end()
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;

int main() {
   int r; cin >> r;
   long double ans = 2 * r * M_PI;
   cout << setprecision(12) << ans << endl;
}
